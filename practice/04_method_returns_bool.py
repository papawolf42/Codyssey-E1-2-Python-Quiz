"""
[실습 04] 메서드에서 bool 반환하기 (return a boolean)

목표:
- 메서드(method)가 현재 객체의 상태를 사용하는 흐름을 익힌다.
- `print`와 `return`을 구분한다.

직접 작성:
1. 생성자에서 `answer`를 저장한다.
2. `is_correct(self, selected)` 메서드를 작성한다.
3. 같으면 `True`, 다르면 `False`를 반환한다.

통과 조건:
- `assert quiz.is_correct(정답) is True`
- `assert quiz.is_correct(오답) is False`

커밋 예: Study: 메서드와 bool 반환 연습
"""

# 이 아래에 직접 작성하세요.
